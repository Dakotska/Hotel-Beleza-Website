function smoothChange() {
    divImages = document.querySelector("images");
    imgLogin = document.querySelector("img-login");
    imgRegister = document.querySelector("img-register");
    console.log(divImages.classList())
    /*if (divImages.classList.contains("login")) {
        divImages.classList.remove("login");
        divImages.classList.add("register");
    } else if (divImages.classList.contains("register")) {
        divImages.classList.remove("register");
        divImages.classList.add("login");
    }*/

}